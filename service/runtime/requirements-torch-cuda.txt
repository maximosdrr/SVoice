# NVIDIA CUDA 13.0 build (Turing/compute capability 7.5 or newer, driver >= 580).
# Installed with --no-deps: shared dependencies live in the base pack.
# torchcodec is only probed by coqui-tts (torch >= 2.9); audio IO uses soundfile.
--index-url https://download.pytorch.org/whl/cu130
torch==2.14.0+cu130
torchaudio==2.11.0+cu130
torchcodec==0.16.0+cu130
